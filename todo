[x] Create a config.py file which will have 
    Style - theme 
    show lines
    background color 

[x] Instead of asking a bunch of question make take arguments
    1st argument is the name of the file, it's location
    2nd start/end positions

[] Create a config.json file which will have that would automatically be read by the program and style the image accordingly. The config file would have the following options:
    Style - theme 
    font size
    show lines
    background color 
    (copy the config class)

[] Create an interface where the program will ask the user to create a config file, if no then just go by default